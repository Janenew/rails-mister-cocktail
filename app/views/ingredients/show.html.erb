<h1 class="text-center"> <%= @ingredient.name %> </h1>

<p> This ingredient is used in the following cocktails : </p>
<ul>
  <% @cocktails.each do |cocktail| %>
      <li>
        <%= link_to cocktail.name, cocktail_path(cocktail) %>
      </li>
  <% end %>
</ul>
<p>
  <%= link_to "<i class=\"glyphicon glyphicon-step-backward\"></i>".html_safe + " Back to cocktail list", cocktails_path %>
</p>
